---
import Base from '../../../layouts/Base.astro';
import LearnList from '../../../components/learn/LearnList.astro';
import { useT } from '../../../i18n';
const t = useT('zh');
---

<Base locale="zh" title={`${t('learn.title')} — NexGrid`} description={t('learn.subtitle')} path="/learn">
  <LearnList locale="zh" />
</Base>
