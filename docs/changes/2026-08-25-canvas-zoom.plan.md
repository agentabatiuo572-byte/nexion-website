# R42 画布整体缩放(主人 2026-08-25 拍板 A:换机制)

Status: **In progress**

## Why

两轮评审(共 8 份报告)的结论收敛到同一个根因:

> 参考站用**一个整体缩放**做到的事,本站用**三百多条声明逐个模拟**。
> 凡逐条模拟就需要一道能核验每条声明的门;而 CSS 表达长度的方式是无穷的,门永远补不完。

硬证据:机制评审把 R39 那场原始事故**换一种写法原样复活,两道门全绿**——门存在的理由被证伪。
两轮分数 3/4 下降,且每轮修复都在造新伤(R41 我一轮造了 4 条)。

## 关键实测:用 `zoom` 而不是 `transform`

主人批准 A 时,我给的代价清单是按 `transform` 估的(要重写钉屏、挪固定层、改坐标逻辑、加 JS 补高)。
动手前实测发现 **`zoom` 更省**,且方向完全一致:

| 边界 | `transform: scale()` | `zoom` |
|---|---|---|
| 影响布局高度 | ❌ 不影响 → 必须 JS 补高 | ✅ **影响** → 零补高 |
| `position: sticky` | ❌ **直接失效**(三种外层写法逐个实测) | ✅ **成立** → 叠卡编舞**不用重写** |
| 画布外 fixed 层 | 需挪出 | 本就在外,不受影响 |
| 居中 | 需 margin 计算 | `margin-inline: auto` 即可 |
| 窄屏 | 需另设 | 媒体查询按**真实视口**生效,断点以下 `zoom:1` 自适应 = 今天的行为 |

⚠️ 唯一注意点:**画布内的视口单位会被二次放大**(实测 `100svh` 在 zoom 1.3333 下渲染成 1333)。
全站仅 4 处用到,逐处改写。

## 目标形态

```
body
├─ canvas#x-bg      ← 固定层,全出血,画布外
├─ .site-nav        ← 固定层,全宽;内层套自己的画布(条内文字随画布缩)
└─ .x-canvas        ← width:1440px; margin-inline:auto; zoom: min(100vw,1920px)/1440px
     ├─ main
     └─ footer
@media (max-width: 1199px) { .x-canvas { width:auto; zoom:1 } }   ← 断点以下=今天的自适应布局
```

一切尺寸回退成**1440 画布上的固定像素**。`--u` / `--uc` / `--x-gut` 三个 token 全部删除。

## 分期

- **P1 地基**:Base 加画布壳 + zoom + 断点;导航内层套画布;验证居中/吸顶/手机端
- **P2 拆脚手架**:329 处 `calc(N * var(--u|--uc))` 回退成 `Npx`;删三个 token;删 main 的留白内边距
- **P3 视口单位改写**:画布内 4 处 vw/vh/svh
- **P4 顺手修四条确认缺陷**:1200 裂缝(P2 后自动消失)、触达 44px(回退后自动恢复)、叠卡编舞脱钩、首屏词梯
- **P5 门重写**:判据从「逐声明形态」换成「画布不变量」——远比现在简单
- **P6 全量复验 + 第三轮送审**

## Done-when

1. 1440 处渲染**逐像素等于 R39 基线**(scale=1,理论恒等,须实测)
2. 1920/2560/3440 下所有尺寸 = 1440 值 × min(vw,1920)/1440,**含字号/盒子/边框/渐变止点/栅格**
3. ≤1199 窄屏与今天**逐像素相同**(zoom:1 + 回退后的固定像素 = 今天的值)
4. 1199↔1200 之间无 16.7% 跳变(裂缝消失)
5. 触达高度全宽度 ≥44px
6. 首屏标题无词梯(载入即正确行数)
7. 叠卡:无死区滚动,末态 scale 收到 0.85
8. 门重写并红测;三语 33 路由零报错零溢出

## Out of scope

- 参考站 1150 以下的第二套画布(另一套设计值,非换基准)
- 排印砍档(12 档 → 4 档)、首屏标题层级倒挂、空屏区块、行长封顶 —— 均为**产品层拍板项**,单独走

---

# 实施结果

Status: **Implemented**(待第三轮评审)

## 与计划的偏差(交底)

主人批准 A 时我给的代价清单是按 `transform` 估的。动手前实测发现 **`zoom` 更省**且方向一致,
于是改用 zoom —— 原计划里「重写钉屏 / 挪固定层 / 改 7 处坐标逻辑 / 加 JS 补高」**一件都没做**,
因为 zoom 影响布局(零补高)且不破坏 sticky(叠卡编舞原样保留)。

## 落地形态

```
body
├─ canvas#x-bg            固定层,全出血(画布外)
├─ .site-nav              固定层,幕布全出血;内层 nav 自套一份画布
└─ .x-frame               全宽裁切框 —— 停放的叠卡裁在**屏缘**
     └─ .x-canvas         width:1440px; margin-inline:auto; zoom: var(--x-zoom)
          ├─ main
          └─ footer
--x-zoom = min(100vw,1920px)/1440px;@media (max-width:1439px) → zoom:1, width:auto
```

断点定在 **1440**:那里画布宽=视口宽、zoom 恰为 1,**两条曲线天然接合,裂缝在构造上不存在**。

## 实测结果

| 项 | 结果 |
|---|---|
| 画布几何 | 2560 → 画布左 320 宽 1920;3440 → 左 760 宽 1920;页高在 1920 以上冻结 |
| 裂缝 | 1439→1440→1441 页高变化 **0.1%**(R41 在 1200 处是 16.7% 硬跳变) |
| 横向溢出 | **33 路由 × 6 档宽度 = 0** |
| 控制台 | 8 档宽度 × 33 路由 **零报错** |
| 最小可见字号 | 11–16px(R41 在 1200–1439 段掉到 10px) |
| 触达 | 全宽度 ≥44 画布像素(R41 曾掉到 36.7) |
| 叠卡 | 吸顶正常、编舞收在 0.85、无死区;单位换算后不再跑快一个缩放倍数 |
| **首屏词梯** | 基线首页 15 个标题中 **13 个是词梯**(标题总高 2858px);修后 **0 个**(1027px)——整页永久虚高约 1000px 的缺陷根治 |
| 内页 1440/390 页高 | 与 R39 基线 **0.0% 相同** |

## 门随机制换代

- **静态门**:从「查每条声明的形态」换成「画布内禁视口单位」——整体缩放下,画布内任何 px 都自动是画布像素,
  「配对值掉队」整族按构造消失,旧判据失去存在理由。新判据小而确定,**6 种危险写法红测全拦、3 种合法写法零误报**。
- **运行时门**:七判据(画布/溢出/等比/冻结/可读/触达/**连续**)。
  🔴 连续判据**从产物 CSS 读出所有断点**逐个探,不写死缝的位置 —— 写死正是 R41 那道缝溜过去的失效模式。
  五条关键判据红测全拦(含「把断点挪回 1200 重现裂缝」)。
- 门在「读不到断点」「sitemap 为空」时**判红而非静默放过**,该设计当轮即救场一次(构建把媒体查询压成了新式区间写法)。

## 本轮我自己踩的坑(留痕)

1. 换算式 `100svh * 1440 / min(...)` 在 CSS 里**是无效值**(长度÷长度得纯数,再减长度非法),整条被丢弃 → 吸顶失效。
   改为抽出 `--x-zoom` 单一真源。
2. 横向裁切放 `body` 无效 —— body 的溢出属性会被**上交给视口**,body 自己反而不裁(实测溢出 5153px)。改用全宽裁切框。
3. 天花板形态转换**目标形式选错**,断点以下丢掉流体行为,手机端页高 +19~28%。用改动前后差异对照逐条改正。
4. 改正又**过度应用**到导航 LOGO 的触达高度上(掉到 32px),被新门当场抓住。
5. 探针没等平滑滚动收敛(本站 1.2 秒缓动),一度误判叠卡坏掉。

---

# R43 收敛(主人 2026-08-25 拍板 A:只修我造的伤,然后停)

## 先修尺子(关键)

新增 `scripts/visual-diff.mjs` —— 逐元素前后比对。

**为什么这是第一件事**:三轮里我每轮都在修好真东西的同时造新伤,而且是同一个模式——
做大范围机械改写后用**总量指标**(页面高度、门全绿)验收,总量对**逐元素回归是瞎的**。
R42 里我第一次改写把手机端页高撑大 19~28%,用页高发现并「修好」;
**那个修法本身把窄屏标题砍掉 44%**,页高恰好看起来正常,于是没被发现。

我本来有这把尺子,但它用 DOM 序号做键,一改结构(插了两层包装)序号整体平移,比对全是噪声;
**我当时放弃了它,而不是修好它**。R43 第一件事就是修好:
- 键 = 标签 + 类名 + 文本前 40 字 + 同签名内序号(插包装层不影响)
- 🔴 **文本键里的数字一律归一** —— 页面上有实时时钟,不归一则该元素被**静默排除**出比对(修的过程中当场踩到,配对数因此从 3451 涨到 3583)
- 采 reducedMotion,排除动画中途态

## 修掉我在 R42 造的四类伤

| 伤 | 实测 | 修法 |
|---|---|---|
| **窄屏展示型字号 −24~47%** | 768 首屏标题 84.5→46.9、页脚巨字 96→51.2;三语同现 | 还原 39 条原写法。根因:`clamp(A,Bvw,CAP)` → `max(A, calc(N*u))` 只在 `B=N/14.4` 时保形,而这些 vw 中项**当初被刻意调得比画布比例大**(窄屏需要相对更大的展示字),23/41 条偏离 >15%,最大 2.78 倍 |
| **白带/页脚丢全出血** | 2560 下两侧各露 320px 黑边,占全页 **41% 滚动长度** | 我删了留白变量却**漏删两处引用** —— 引用未定义变量会让整条声明**静默作废**。改用 `--x-bleed`(屏幕量 ÷ 画布缩放 = 画布量) |
| **点站内链接标题被导航压住** | ≥1440 净空 **−16.1px** | 写死的屏幕常数 vs 会随画布缩的 CSS 值。改为实测导航条高度。**同族还有两处**(移动菜单常数、视差位移)一并扫掉 |
| **导航羽化带 ≥1920 缩水 25%** | 21.33 → 16px | 幕布在画布壳外却用画布单位 ⇒ 被冻结在 1440 值。改为显式乘缩放 |

🔴 **修法本身也用尺子验**:还原 39 条后系统性核对「R41 的每条 clamp 是否原样在位」——
逐个碰运气会漏(实测确实漏了手机端叠卡列距那条)。

## 门的收口(按评审逐条)

**静态门**:上一轮评审 8 例注入放行 6 例。R43 后 **12 拦 + 5 放,零误报**:
- `@media` 从「有就豁免」改成**读条件本身**(只有 `max-width ≤1439` 才豁免;`min-width:1441` 恰是缩放区内部、最该查)
- 视口单位表补全(大小写不敏感 + 逻辑/small/large/dynamic 前缀)
- 豁免判定从**按行**降到**按声明**(同行放个换算不能给别的项发通行证)
- 新增「上限夹得住的 clamp 放行」判据(`B×14.4 ≥ CAP` 时视口项在画布内永不生效)——
  🔴 **这条是被自己的门逼出来的回归教训**:R42 一刀切禁视口单位,直接导致上面那条 −44%
- 🔴 **新增死引用判据**:引用未定义且无兜底的自定义属性即判红。纯静态、零误报,正是白带黑边那一族

**运行时门**:
- **溢出判据换信号** —— `.x-frame` 的裁切让 `scrollWidth` 恒等,原判据是**恒真空转**(评审注入超宽卡越屏 480~1430px,判据报 0)。改为逐元素越界 + 祖先裁剪感知(内层容器裁=合法,画布框/主体裁=真缺陷)。红测:注入超宽卡当场报 1410px
- **画布判据对齐 CSS 口径**:`--x-zoom` 用 `100vw`(含滚动条)而门用 `clientWidth`(不含)—— 两个模型。改为同源,并新增「画布不得超出可视区」判据
- **成功行按实跑集合生成**,并打印实测服务地址(此前自述了没跑过的宽度)
- **未执行不再写绿**:此前人读的说「未执行不算过」而机器读的退出码文件写 0,两条结论相反

## 最终复验(逐元素,全 33 路由)

| 宽度 | 配对元素 | 变化 |
|---|---|---|
| 390 | 3574 | **0** ✓ |
| 768 | 3583 | 3(全是 0.1px 绘图取整) |
| 1024 | 4120 | 165 —— **全部 5 组,全是有意改动**(导航条高 + 触达键高) |
| 1440 | 4285 | 165 —— 同上 |

八门全绿,退出码 0。

---

# 收尾:PRD 同步判定与进化闭环

## PRD 同步判定:**不同步**

官网 PRD(`PLAN/PRD/NexGrid_官网PRD_v1.0.md`)无需改动。判据:R42-R43 全程为
**渲染机制换代 + 缺陷修复 + 机器门重写**,未动任何功能契约 —— 页面入口、路由、i18n key 树、
下载/H5 链接的降级规则、信任口径、SKU 事实一律未变;`verify` 的禁用词门与三语 parity 门全绿可证文案面零改动。
(工作流要求:同步与否都要写明结论与理由,省掉的是询问不是透明度。)

## 进化闭环:本轮唯一真正新的教训已归层

**总量指标掩盖逐元素回归** —— 连栽三轮,每轮换一种改写口径、栽法完全一样。
- **机器层**:`scripts/visual-diff.mjs` + 仓规「机械改写后必跑逐元素回归」
- **心智层**:memory `feedback_aggregate_metric_hides_per_element_regression`,并挂进错题索引

判据一句话:**改了 N 处就要验 N 处;用一个数验一百处,那个数一定测不到你真正改坏的那一处。**

## 未处理项(产品决定,已交主人拍板)

首屏标题比原版小 38%(需先定文案能否缩短)· 排印梯级 16 档 vs 原版 4 档、粗体等宽 111 处 vs 2 处 ·
1440 以下不全缩(要做需再画一套画布)· 信任证书屏上字高约 3px 且无放大出口 ·
五卡 4+1 孤儿行 · FAQ 右缘内缩 347px · 正文行长 104~185 字符且全大写 · 页脚中段空 743px。

---

# R44 产品层收尾(主人 2026-08-25 拍板「2 4」)

## 第 2 项 · 排印砍档

| 项 | 改前 | 改后 | 原版 |
|---|---|---|---|
| 展示字档位 | 17 | **9** | 4 |
| 等宽字档位 | 4 | **2** | 2 |
| 粗体等宽 | 141 处 | **80 处** | 2 处 |

**并档口径**:每步移动 ≤16%(不引起重排);真正的「看不出层级、只看得出不齐」集中在
26/25/24/23/21 五档挤在一起的小端 —— 那里合成一档。
`64/51.84/48 → 54` · `26/25/23/21 → 24` · `20.16 → 20` · `13/14 → 12`

**没做到 4 档的交底**:原版只有 4 档,是因为它的内容类型少(没有长文、法务页、规格表)。
真做到 4 档需要**逐页重排标题层级**,是另一档量级的设计工作,主人未批。
剩余 9 档里 120=品牌巨字、100.8=首屏(第 1 项未批,保持)、88=内页标题、28=LOGO 字标。

**粗体**:`.x-label`(全站用得最多的等宽标签类)700→400,是「粗体从唯一强调手段退化成底噪」的主因;
另 10 处组件级 700→400。保留转化按钮与正文语义强调。
剩余 80 处 = 按钮箭头(每个按钮三层滑切 × 约 25 个按钮)+ 5 处正文 `strong`,**口径上已是「只给转化按钮」**。

## 第 4 项 · 证书放大出口

此前证书在屏上字高约 3px 且**没有任何放大出口**,而信任区唯一的作用就是让人核验。

- 图片包进 `<button>`(键盘可达)+ 悬停/聚焦提示
- 点开用**原生 `<dialog>`**:Esc 关闭、焦点管理、背景遮罩全部由浏览器负责,零框架
- 逻辑按仓规住进交互引擎单文件,不留组件内联脚本
- 实测:2560 缩略 417px → 点开 **880px(2.1 倍,原生尺寸)**;1440 312 → 660;三语通过,Esc 可关
- ⚠️ 390 手机端只能放到 342px(1.1 倍)—— 屏幕物理限制,已尽最大可用宽度

## 验证

- 逐元素回归(390/1440/2560 × 33 路由):变化**全部落在有意并的档上**,无意外元素类
- 🔴 **新门当场抓住我自己的四处违规**:对话框里写的三处画布内视口单位 + 一处并档后上限夹不住的 clamp。
  门在自己作者身上生效,是它可信的最好证据。
- 八门全绿,退出码 0

## 仍未处理(主人已判定不做 / 未选)

第 1 项 首屏标题放大到原版尺寸(需先缩短英文/越南文文案)· 第 3 项 窄屏第二套画布(**主人采纳我的建议:不做**)·
第 5 项 五卡 4+1 孤儿行 / FAQ 右缘内缩 347px / 正文行长 104~185 字符且全大写 / 页脚中段空 743px。

## R44 的 PRD 同步判定:**需要同步,已同步**

与 R42/R43 的「不同步」不同 —— R44 加了**用户可见的新交互**(证书点开原尺寸)与新文案 key。
官网 PRD 的 `[FEAT-WEB07] 信任板块` 有一张**点击流矩阵**,纪律是「每个可点元素一行、严禁点了没反应」,
新增可点元素后那张表就不完整了。已同步两处:
- 点击流矩阵 +1 行:`注册证书图 → 当前区 → 点开原尺寸大图(键盘可达;Esc / 点背景关闭)`
- 交互 4 态的「默认」补上口径:**本板块的作用是让人核验,缩略图上的编号不可读即等于失效**

(只写功能与交互,不写视觉规格 —— 对话框样式、缩略图尺寸不进 PRD。)
