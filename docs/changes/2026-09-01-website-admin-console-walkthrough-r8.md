# 官网后台控制台 · 真浏览器实景走查(第八轮:补走 5 项 + 回归 12 条修补)

**判决 = 不可签字**(仅就界面实景而言)。

上一轮明说没走到的 5 个面本轮全部走完,其中**执行器真在线跑完一次完整发布**(5 分 30 秒,四步逐一推进)全程通过;
12 条修补里 **17 个子断言实景证实、3 个部分成立、2 个实景推翻**。不可签字的原因是三条 P1:
① 后端整体不可达时控制台永久停在骨架屏(**这一轮修「未登录先确认会话」时新引入的回归**);
② 产品卡的「事实字段」(名称/价格/算力倍数/状态)可以**零理由**发布上线——高敏判定的正则写的是点号路径,而真实路径是方括号,那个分支**从来没生效过**;
③ 审计日志「对象」列仍直印 `unknown`(上一轮 P1-5b,不在本轮修补清单内,未修)。

---

## 〇、怎么验的(证据面交底)

| 项 | 值 |
|---|---|
| 工作目录 / HEAD | `D:\WORKS\PLAN\.wt\w-console`,`pkg/w-publish` @ `f21bc6d` |
| 产物 | 仓根 `npm run build` → `npm run build:console` → `node worker/promote.mjs`(`dist-live` 112 文件 / `1867d1a69e6dc26b`) |
| 迁移 | `wrangler d1 migrations apply nexgrid_site --local --persist-to <独立目录>`,0001–**0007** 全绿 |
| 服务 | `npx wrangler dev --port **8823** --persist-to <独立目录>`;控制台 `http://127.0.0.1:8823/admin/`。8823 未被占用,未退到 8825 |
| 隔离 | D1/KV 全落在 `…/scratchpad/wstate8`,**未碰** `worker/.wrangler/`;禁用端口一个没占(4399 是别的会话的,全程未动) |
| 渲染判断面 | **Playwright**(借 `Nexion-uniapp` 依赖),`--disable-features=WebContentsForceDark`;结论一律 DOM / 计算样式断言,截图只作佐证 |
| 冻结 | 全程 `worker/src/**`、`worker/*.mjs`、`admin/src/**`、`src/components/legal/**` 只读——收尾 `git diff --stat` 对这四面为空,`git status` 完全干净 |
| 证据 | 68 张截图 + 17 份 JSON,在 `C:\Users\jason\AppData\Local\Temp\claude\D--WORKS-PLAN\77632ed7-c975-44f2-9faa-7d4a48266c50\scratchpad\`(`shots8/` + `r8-*.json`),不在仓库内 |

**本轮撤回的两个自己的误判**(如实记录,防止下轮再踩):

| 我一度以为 | 真相 | 判据错在哪 |
|---|---|---|
| 全站 14 组样式对比度不达标(第一版探针报 30+ 处红) | **293 组取样零失败** | 探针把 `rgba(…, 0.12)` 半透明底当不透明色算亮度。修正为「自根向下逐层 alpha 合成」后全绿 |
| 451 拦截页是**空白页**(Content-Length: 0) | 双语拦截页 879 字节,内联样式渲染正确 | 我用 curl / `page.request` 打的,`Accept: */*`,走的是**资源分支**;文档请求(`Accept: text/html`)才出 HTML。这是设计如此,不是缺陷 |

**界面状态怎么摆的**(只动本地 D1 / KV / `dist-live` 印记,不动代码):

| 状态 | 手法 |
|---|---|
| `/setup` 四态 | **全新未初始化的 D1**——这是这个页面唯一的机会窗,初始化后就再也回不去 |
| 真发布 | UI 里改 `stats.nodes` 156→162 存草稿 → UI 里点发布(高敏须理由)→ 另一终端 `npm run publish:runner -- --api http://127.0.0.1:8823 --cookie … --once` |
| 区域屏蔽真数据 | UI 里开启屏蔽 + CN → 用 `x-geo-sim: CN` + `Accept: text/html` 打 17 次文档请求(真 451、真落 `raw_events`)→ `POST /api/admin/rollup` 汇总进 `daily_blocked` |
| 区域屏蔽 E2 误伤护栏 | 直接写 `daily_traffic` 给 VN 造近 7 天流量,再从 UI 把 VN 加进名单 |
| 审计长值 | `--file` 灌一条 243/261 字的 `before/after`(避开 Git Bash 中文参数坑) |
| 版本表溢出 | 递归 CTE 灌 45 条历史版本(混 archived/failed/cancelled,payload 有的能算改动数有的是 `{}`),共 47 版 |
| 改动数基线验证 | 把 v17 改成与 v18 差 5 个字段,看界面给 v18 印几处 |
| 劈叉(内容被改) | 把 `dist-live/.publish-stamp.json` 里两条 anchor 摘要改成全 0(验完已从备份还原) |
| E1 前置校验红 | `json_set` 把 `downloads.{ios,android}.enabled` 置 true 而 URL 留空 |
| 发布中三档取消 | UI 里发起一次回滚 → 排队(`yes`)→ 灌 `publish_steps` 变 `no` → 把步骤时间戳往前推 13 分钟变 `force` |
| 壳层报错态定性 | 分别 abort/500 `**/api/**`、只 abort `/api/me`、只 abort `/api/config`、只 abort `/api/dash`,四组对照 |

---

## 一、补走上一轮明说没走到的 5 项

### 1. `/setup` 初始化页四态 —— **全部 pass**

| 态 | 判 | 证据 |
|---|---|---|
| 默认 | pass | `h1`=「初始化管理员口令」;三个字段(令牌 / 口令≥12 位 / 重复),令牌框 `autoFocus`,两个口令框 `autocomplete="new-password"`(**符合 CON01-⑤「防自动填充泄漏」**);`body` 背景 `rgb(10,10,11)`;溢出 0;markdown 0;console 0 |
| 空状态 | n/a | 该页无数据面 |
| 加载 | pass | 提交中按钮文案 `设置口令`→`提交中…`、`disabled=true`、0 个转圈;不白屏。请求失败后按钮复位并显示「网络异常,请重试」 |
| 报错 / 极限 | pass | 口令 <12 位 →「口令至少 12 位」(纯前端,不发请求);两次不一致 →「两次口令不一致」;**错令牌 → 服务端 403 →「初始化令牌不正确」**,不跳页、字段值保留;console 0 |
| 成功路径(A1) | pass | 正确令牌 + 12 位口令 → toast「初始化完成,请登录」→ `replace` 跳 `/admin/login`;console 0 |
| E4(已初始化) | pass | 再访问 `/admin/setup` → 死卡「已初始化 / 本后台已完成初始化,此入口永久失效。忘记口令须重新部署轮换 SETUP_TOKEN(见 worker/README 运维手册)」;**表单 0 个、输入框 0 个、无任何重置入口**,只有一个「去登录」。服务端 POST 同时回 410。**PRD CON01-E4 完全满足**,上一轮记的「立即重定向到 /login」不成立(那是初始化成功后的跳转,不是 E4) |

**顺带抓到一条 PRD 差**:CON01-⑤ 写「空状态:未初始化访问 /login → 引导文案『先完成初始化』(**不渲染表单**)」。
实景:未初始化时 `/login` 渲染**完整口令表单**,和已初始化时**一模一样**,只有页脚一行小字提到「去初始化 →」。见 P2-11。

### 2. 执行器真在线跑完一次完整发布 —— **全部 pass**(本轮最有价值的一段)

一次真发布 **5 分 30 秒**(比 README 说的 15 分钟快,门链本身 5 分 24 秒)。每 15 秒采一次界面,时间轴:

| t | 界面实景 |
|---|---|
| 0s | 四步全 `等待` + 「排队中——发布执行器尚未领取任务…」+【取消本次发布】 |
| 2s | `完成 物化配置` / `进行中 站上全部机器门(13 门) 已耗时 26 秒` / `等待 生产构建` / `等待 原子切换上新` |
| 35s | 门步 `已耗时 **1 分 0 秒**` ← 跨过 60 秒边界,单位自动换档 |
| 69s / 186s / 286s | `1 分 33 秒` / `3 分 31 秒` / `5 分 11 秒`,每 15 秒稳定递增 |
| 305s | `完成 门(5 分 24 秒)` / `完成 生产构建(3 秒)` / `进行中 原子切换上新` |
| 321s | 进度块消失;版本表 `v2 … **线上** … 1 处`、`v1 … 历史 … —` 且 v1 长出【回滚到此版】;状态条 `线上 v2` + `与线上一致` |

| 态 | 判 | 证据 |
|---|---|---|
| 默认(发起前) | pass | 「本次发布 · 1 处改动 **含高敏字段,须填理由**」;确认面要求理由(≥8 字);toast「发布已发起」 |
| 加载(发布中) | pass | 四步齐全、当前步高亮、>2s 显示已耗时、**耗时用人话单位**、按钮禁点;整段 console **0**、失败请求 **0** |
| 空状态(发布后) | pass | 「没有待发布的改动(草稿与线上一致)」,发布按钮 disabled + 「无改动可发布」 |
| 报错 / 极限 | pass | 门跑到一半时线上仍是 v1(`dist-live` 未被换),切换后才变 v2;`.publish-stamp.json` 写入 38 条 anchor + 一次性口令,服务端回读核验通过才标 live |

执行器日志:`▶ 领到发布任务 v2` → `✓ v2 已上线(dist-live 已更新)`,exit 0。

### 3. 区域屏蔽卡在有真数据时的样子 —— **全部 pass**

| 面 | 判 | 证据 |
|---|---|---|
| geo 页统计区(有数据) | pass | 「今日拦截(实时) **17**」/「近 7 天拦截 **17** 占总请求 **0.9%**」/「被拦区域 TopN:**CN 中国大陆 17**」;溢出 0;markdown 0 |
| 驾驶舱屏蔽卡(有数据) | pass | 运营健康卡「**17** 被屏蔽请求 · 占 0.9%」+「**1 地区**」+「屏蔽规则 详情 →」;卡下一行「被拦 Top:CN 17」;今日行「PV 0 · 点击 0 · **被屏蔽 17**」;16 张卡,溢出 0 |
| 空态 → 有数据的过渡 | pass | 启用前是「屏蔽未启用,暂无数据」;启用后零命中时是「暂无拦截记录」;有命中后才出数字。**三档分得清**,没有把「未启用」画成「0 次拦截」 |
| 开启动作(A2) | pass | 勾总开关 →「改动未应用」→【应用变更(确认+理由)】→ 确认面「目标:开启,名单 [CN]」+ 理由 ≥8 字 → toast「已写入并回读确认 · 约 1 分钟内全球生效」→ 状态徽标「已生效 · 10:57:52 回读确认」 |
| E2 误伤护栏 | pass | 把 VN 加进名单 → 红条「⚠ 误伤护栏:**VN 越南 占近 7 天流量 77.7%**——这是主要市场流量」+ 必勾「我知道这会拦截主要市场流量」;**未勾时【仍然应用】`disabled=true`**,勾上才解禁 |
| 451 拦截页本体 | pass | 文档请求返回 879 字节双语页(zh 标题+正文 / `<hr>` / en 标题+正文),`<style>` 全内联、`meta robots=noindex`、**零外部资源**;资源请求(非 `text/html`)返回空体 451,不浪费带宽 |

### 4. 审计页 CSV 导出 —— **pass**(内容对,但列口径与屏幕不一致,见 P2-10)

| 项 | 实测 |
|---|---|
| 文件名 | `audit-2026-09-01.csv` |
| 体积 / 行数 | 2935 字节 / 17 行(表头 + 16 行,与屏幕上 16 行一致) |
| 编码 | **带 UTF-8 BOM**(`EF BB BF`)——Excel 直接打开中文不乱码,这一步做对了 |
| 表头 | `id,time,actor,action,target,before,after,reason` |
| 转义 | 每个字段都加双引号,内部引号 `""` 双写;243/261 字的长 Legal 摘要**完整落盘未截断**,中文完好 |
| 反馈 | toast「已导出 16 行(**当前已加载范围**)」——口径诚实,没假装导出了全部历史 |
| 过滤后导出 | 切到「运维」(1 行)后导出 → 1 行,toast 同步说 1 行 |

### 5. 文案树 18 个命名空间逐个展开 —— **全部 pass**

18 个分组全部点开,每组都有 key、都能渲染三语编辑框,**没有一个空分组**;全程 console 0、失败请求 0、横向溢出 0、markdown 记号 0、禁用词红 0、缺译 0。

| # | 分组 | key 数 | 文本框 | 备注 |
|---|---|---|---|---|
| 0 | 首屏 Hero | 6 | 18 | |
| 1 | 下载按钮文案 | 2 | 6 | |
| 2 | 统计标签 | 6 | 18 | |
| 3 | 收益路径 | 11 | 33 | |
| 4 | How it works | 13 | 39 | |
| 5 | 设备板块 | 7 | 21 | 集合背书提示「各设备标语随**产品卡**编辑」 |
| 6 | 信任板块 | 14 | 42 | 全组 14 个 key 各带「高敏 · 发布须理由」 |
| 7 | 社证 | 3 | 9 | |
| 8 | 使命 | 9 | 27 | |
| 9 | NEX | **58** | **174** | 最大的一组,仍无溢出 |
| 10 | 学习中心 | 16 | 48 | |
| 11 | FAQ 标题 | 1 | 3 | 集合背书提示「问答条目在 **FAQ 模块** 管理」 |
| 12 | 收尾 CTA | 1 | 3 | |
| 13 | 站点信息(SEO 源) | 3 | 9 | |
| 14 | 导航 | 11 | 33 | |
| 15 | 页脚 | 12 | 36 | `footer.legalLine` 单 key 高敏 |
| 16 | 法务提示 | 1 | 3 | 高敏组 |
| 17 | 404 页 | 3 | 9 | |

---

## 二、本轮 12 条修补逐条核实(拆成 22 个子断言;主人说的「13 条」按列出的条目数是 12,其中两条各含三个子项)

| # | 断言 | 判定 | 实景证据 |
|---|---|---|---|
| 1a | markdown 星号 5 处已改 `<b>` | **实景证实** | 12 个页面 + 劈叉面 + 强制中止面全部 DOM TreeWalker 扫描,`/\*\*[^*]+\*\*/` **0 命中**。截图里「这一版记为**已取消**」「**并不会去停掉那个执行器进程**」肉眼可见是粗体,无星号 |
| 1b | 焊了 `gate-console-copy.mjs`(两条判据 + 8 条自检,接进执行器门步与 npm 脚本) | **实景证实** | `node gate-console-copy.mjs` → exit 0「扫了 16 个页面文件,零命中」;`--self-test` → **8/8 全绿** exit 0;`git ls-files` 显示已跟踪(提交 `dcce601`);`worker/runner.mjs:81-92` 把它接进门步循环,`worker/package.json:22` 有 `gate:console-copy`。**覆盖边界**:它不在站仓 `npm run verify` 的 13 门里,只在**发布时**跑;判据②有盲区(见 P2-13) |
| 2 | 审计加载失败不再印「没有匹配记录」 | **实景证实** | abort `/api/audit` → 页面只有「加载失败 重试」+「这一段记录当前取不到,不代表没有发生过。请点上方「重试」。」;`没有匹配记录` **不出现**,骨架屏 **0** 个(既不说谎也不假装还在加载) |
| 3a | 版本表截断会明说 | **实景证实** | 47 个版本时出「只显示最近 31 条(更早的版本仍在,未删除)。当前线上那一版已单独固定显示在列表里。」 |
| 3b | 当前线上那一行单独固定显示 | **实景证实** | 窗口是 v47→v18(30 行),**v2(线上)被单独钉在表尾第 31 行**,不再消失 |
| 3c | 补了「改动数」列(算不出来留空) | **部分成立** | 表头有「改动数」,`{}` payload 的版本正确留空 `—`。但两处算错/算不出:**线上那一行恒为空**,且**窗口最旧那一行的基线是被钉进来的线上版而非它的真前驱**——把 v17 改成与 v18 差 5 个字段后,界面仍给 v18 印「1 处」(那是 v18 vs **v2** 的差)。见 P2-4 |
| 4 | `.kv` 文字色 #5c5c64 → #94949d | **实景证实** | 12 个页面 **293 组取样样式,对比度失败 0**;`.kv` 实测 `rgb(148,148,157)`,最差底比值 **6.17**。旗舰用例「为什么这个按钮不能点」:禁用的【发布(过全部机器门)】+ 旁注「无改动可发布」实测 **5.70**(原 2.59) |
| 5a | 审计动作附机器码小字 | **实景证实** | 动作列主视线是「登录成功 / 保存草稿 / 修改区域屏蔽规则 / 发布成功上线 / 发起发布 / 完成初始化 / 手动重算统计」,机器码 `login.success` 等以 `.kv.mono` 小字挂在下方 |
| 5b | 产品卡 | **实景证实** | 7 行全部是「在售 / 已停产」,`active`/`legacy` 不再出现;编辑面 `<select>` 的 option 文字也改成中文,value 保留机器值并标 `enum-ok` |
| 5c | 公告条 | **实景证实** | **五个窗口态逐个验过**:未启用(灰)/ 缺起止时间(黄)/ 已排期(还没到展示时间)(黄)/ 展示中(绿)/ 已过期(展示时间已过)(黄)。`disabled`/`scheduled`/`expired`/`live` 一个都不再出现 |
| 5d | 下载入口 | **部分成立** | 徽标已改成「未配置(站上显示「即将推出」并禁用)」✓,但**同一页另外两处、加服务端校验消息一处,`coming-soon` 原样还在**。见 P2-3 |
| 5e | HTTP 状态码与 select 的 value 给 `enum-ok` 逃生阀 | **实景证实** | `downloads.tsx:55`「可达 {p.status}」与 `skus.tsx:75` 的 `<select value={s.status}>` 各带一条 `enum-ok` 注释;门对这两处不报,对裸 `{r.status}` 仍报(自检第 4/6/7 条覆盖) |
| 6 | 状态条与劈叉红条不再同屏矛盾 | **实景证实** | 造 tampered 印记且草稿零改动 → 四个页面(`/publish` `/` `/audit` `/content/legal`)状态条一致显示「**草稿无改动(线上内容另有问题,见上方红条)**」,`与线上一致` 不再出现;红条同屏也不矛盾 |
| 7a | 「去发布」在发布页显式禁用 | **实景证实** | `/publish` 上是 `<button disabled title="已经在发布页了">`;其它页仍是可点的 `<a href="/admin/publish">` |
| 7b | 窄窗导航补 aria-label/title | **实景证实** | 768/899/1000/1023 四档下 **12/12** 导航项都有 `aria-label` 与 `title`(「驾驶舱」「文案树」「下载入口」…),读屏器不再读到空名 |
| 7c | 折叠断点 900→1023 | **实景证实** | 1024 → 侧栏 216px、12 个标签可见;**1023 → 侧栏 64px、标签全隐**。与 PRD CON02-⑤「窄窗(<1024px)」严格对齐。四档均无横向溢出 |
| 8 | 「已耗时 881s」→「14 分 41 秒」 | **实景证实** | 真发布里从 `26 秒` → `1 分 0 秒` → `5 分 11 秒` 连续观测;强制中止场景观测到 `13 分 52 秒`。`s` 后缀一次都没再出现 |
| 9a | 未登录时先确认会话再挂子页 | **实景证实(功能)/ 引入回归(报错态)** | 未登录打开 `/admin/` → console **1 条**(上一轮 8 条)、失败请求 **0**、`ERR_ABORTED` **0**;`/api/dash` 那条越权请求**不再发出**。**但同一处改动让「后端整体不可达」变成永久骨架屏 → P1-1** |
| 9b | 401 改用路由跳转且一次加载只跳一次 | **实景证实** | 只观测到一次 SPA 路由跳转到 `/admin/login?back=%2F`,无整页重载、无连串取消导航 |
| 10 | 「能不能取消」由服务端在 `/status` 直说 | **实景证实** | 三档全验:排队 `cancelable=yes`(【取消本次发布】可点);已开工且有动静 `no` → 点【中止本次发布】只出 toast「执行器仍在工作(最近还有步骤动静),现在中止会留下没人收口的中间态」,**新增 HTTP 错误 0 条、console 错误 0 条**;失联 13.9 分钟 `force` → 直接出强制中止面,**同样零试探请求**。短理由被拒、合法理由中止成功、版本转「已取消」、锁释放 |
| 11a | 三处不再直印字段路径(共享映射表,原始路径降小字) | **部分成立** | 三处都变成「人话 + 11px 小字原始路径」✓。但「人话」的尾巴仍是字段名,数组路径下更是把**整条原始路径原样保留**:实景抓到 `产品卡 · skus[0].priceUSD`,小字重复同一串。且审计页完全没用这张表。见 P2-2 / P2-8 |
| 11b | 「去修复」带 `?focus=<字段>` | **实景推翻** | 链接确实是 `/admin/content/downloads?focus=downloads.ios` ✓,但**目标页没有任何消费者**:点击后 `scrollY=0`、`activeElement=BODY`、focus 环 0 个、高亮元素 0 个、红边框输入框 0 个;全仓 grep 无一个 `useSearchParams`/`location.search` 读它。**这个参数是摆设**。见 P2-1 |
| 12a | 审计长值补体量提示 | **实景证实** | 折叠态 `max-width:320px` + `nowrap` + `ellipsis`,尾部挂「· **1.4 KB,点击展开**」;点开后全文 507 字、提示消失、页面不横向溢出 |
| 12b | 驾驶舱骨架屏改成 4+3+3 贴合真实布局 | **部分成立** | 骨架确实是 4/3/3 三行 10 张卡、20 条骨架、0 个转圈(上一轮是一排 4 条挤在同一 y)。但真实布局是 **4/3/3/4/2 五行 16 张卡**,骨架高 900 vs 真实 1129——覆盖了 10/16 张卡,落地时仍跳 229px。见 P2-7 |
| 12c | chip 超长真截断 | **实景证实** | DOM 注入 80 位版本号:宽度从 70px 只涨到 **308px 就停住**(`max-width:42ch`=307.87px),`text-overflow:ellipsis` 生效、`scrollWidth>clientWidth`、高度仍 30px 不换行、同排另两个 chip(87/93px)**没被顶开**、文档无横向溢出 |

**小计**:22 个子断言 → **17 实景证实 · 3 部分成立 · 2 实景推翻(11b 完整推翻;9a 功能成立但引入回归)**。

---

## 三、P0

**0 条。** 没有发现让操作者把坏内容发上线、或让界面完全不可用的缺陷。真发布链、门链、上线核验、回滚、中止、劈叉自查全部实景通过。

---

## 四、P1(逐条不合并)

### P1-1 🔴 后端整体不可达时,控制台**永久停在骨架屏**——没有「状态获取失败」、没有重试按钮、子页不挂载

- **页面**:全部页面(壳层)。
- **现象**:`/api/me` 拿不到答复(网络断 / worker 挂 / 500)时,状态条渲染成一条骨架、内容区渲染成两块骨架,**永远不变**。屏幕上没有任何字说明出了问题,也没有任何控件能重试;只有浏览器手动刷新能重来。
- **复现**(四组对照,判据干净):

  | 拦什么 | 「状态获取失败」 | 重试按钮 | 骨架 | 子页挂载 |
  |---|---|---|---|---|
  | `**/api/**` 全 abort | ✗ 无 | 0 | 3 | 否 |
  | `**/api/**` 全 500 | ✗ 无 | 0 | 3 | 否 |
  | **只** abort `/api/me` | ✗ 无 | 0 | 3 | 否 |
  | 只 abort `/api/config` | ✓ 有 | 1 | 0 | **是** |
  | 只 abort `/api/dash` | ✓(页内) | 1 | 0 | 是 |

  即:报错态只在「会话探针恰好成功、别的接口才挂」时才成立;**整个后端不可达这个最常见的情形反而没有报错态**。
- **影响**:PRD CON02-E1 白纸黑字写「Given API 不可达,When 加载壳,Then 状态条显示『状态获取失败 · 重试』按钮,导航仍可用,不白屏」。导航确实还在(12 个链接可点),但**重试出口没了、错误提示没了**,运营看到的是一个永远转不完的加载。上一轮这一条在每个页面都是 pass,是**这一轮修 9a 时新引入的回归**。
  更要紧的是:**同一批修补里的第 2 条**(审计页)的注释原话是「既不显示『没有匹配记录』(那是在说谎),也不**永远转骨架屏(那是在假装还在加载)**」——壳层刚好犯了它自己判定的第二种错。
- **根因**:`admin/src/shell.tsx:56-60`
  ```
  api('/api/me')
    .then(() => { setAuthed(true); reload(); })
    .catch(() => {}); // 401 已由 api 层跳转;其它错误由下面的失败态兜
  ```
  注释里说的「下面的失败态」由 `failed` 驱动,而 `failed` **只在 `reload()` 的 catch 里被置位**(`:45-50`),这条路径上 `reload()` 根本没被调到。于是 `authed` 恒 false(`:149` 渲染骨架代替 `<Outlet/>`)、`overview` 恒 null 且 `failed` 恒 false(`:136-138` 渲染骨架代替重试块)。**注释断言了一个不存在的兜底。**

### P1-2 🔴 产品卡的「事实字段」(名称 / 价格 / 算力倍数 / 状态)可以**零理由**发布上线——高敏判定那条正则匹配的是一种不存在的路径写法

- **页面**:`/content/skus` 与 `/publish`(两页对同一字段说法相反)。
- **现象**:把 SKU 价格从 $4,321 改成 $9,999 后:
  - 产品卡页标题写「产品卡 **事实字段高敏**」;
  - 发布页写「本次发布 · 1 处改动」——**没有**「含高敏字段,须填理由」徽标,diff 行判为「**普通**」;
  - 确认弹窗里**连理由输入框都没有**(`textarea` 数=0),【确认】直接可点;
  - 发布后审计只留下「发起发布 / v49 / live=v2 → 1 处改动 / 理由:**空**」。
- **复现**:登录 → 产品卡页把任一 SKU 的价格改一个数 → 保存草稿 → 发布页点【发布】。或直接看接口:`GET /api/publish/preflight` 回 `changedPaths:["skus[0].priceUSD"]`、`sensitiveChanged:[]`、`reasonRequired:false`、`ready:true`。
- **影响**:官网上公开展示的产品事实(型号名、价格、算力倍数、在售状态)改了之后,「谁改的、为什么改」在审计里查不到——而这正是 CON14 存在的理由。控制台自己在产品卡页把这些字段标成高敏,却在唯一执行这条规则的地方放行,**界面对操作者说了两句相反的话**。所有 13 道机器门照跑不误,所以坏内容仍上不了线;丢的是**归因**。
- **根因**:`schema/src/validators.ts:164`
  ```
  /^skus\.[^.]+\.(name|priceUSD|multiplier|status)/.test(p)
  ```
  这条正则要求 `skus.<键>.priceUSD`(点号)。而路径由 `schema/src/diff.ts:10` 的 `flattenJson` 生成,数组走的是**方括号**:`v.forEach((x, i) => flattenJson(x, \`${prefix}[${i}]\`, out))` → 实际路径是 `skus[0].priceUSD`。`^skus\.` 遇到 `skus[` 立刻失配,**这个分支从来没有生效过**。
  下游一路照单全收:`worker/src/publish.ts:274` `reasonRequired: sensitive.length > 0`、`:311` 的 400 拦截、以及界面上的高敏徽标与理由框,全部据此关闭。
- **注**:同一条 `sensitivePaths` 里 `downloads.` / `stats.` / `legal.` 三个前缀写的是对象路径,实测生效(本轮 `downloads.ios.enabled` 正确判为高敏)。**只有数组那一支是死的**。

### P1-3 🔴 审计日志「对象」列仍直印 `unknown`(16 行里 9 行)

- **页面**:`/audit`。
- **现象**:动作列已经改成人话了,紧邻的对象列上,每一条登录记录都写着 `unknown`。屏幕实景:9 行 `unknown`,另外 7 行是 `legal.terms.zh` / `2026-09-01` / `geo:rules` / `v2` / `draft` / `admin`。导出的 CSV 里同样是 `unknown`。
- **复现**:登录 → `/admin/audit`,打开即见(截图 `audit-0-default.png`)。
- **影响**:审计是追责面,「对象」这一列的作用是回答「改的是什么」。对登录事件它写的却是请求方 IP,本机开发下取不到 IP 就退化成机器占位词 `unknown`。项目不变量写着「页面文案禁止工程名词 / 字段名 / 枚举值」。上线到 Cloudflare 后这里会变成一个**真实 IP 地址**——那既不是「对象」(登录事件的对象是账号),又把访问者 IP 摆到了列表主视线上,是另一个需要拍板的问题。
- **根因**:`admin/src/pages/audit.tsx:91` `<td>{r.target ?? '—'}</td>` 直出;值来自 `worker/src/auth.ts:208/214` 的 `writeAudit({ action:'login.success', target: ip })`,而 `clientIp()`(`auth.ts:60`)在没有 `cf-connecting-ip` 头时返回字符串 `'unknown'`——**是真的写了 `'unknown'` 进库,不是空值**,所以 `?? '—'` 兜不住。
- **注**:上一轮把这条记为 P1-5b,本轮 12 条修补清单里没有它。我按上一轮的定级保留 P1,不因为「这轮没打算修」而调低。

---

## 五、P2(逐条不合并)

### P2-1 「去修复」带的 `?focus=<字段>` 是个摆设,目标页没有任何消费者

- **页面**:`/publish` → `/content/*`。**现象**:链接是 `/admin/content/downloads?focus=downloads.ios`,点击后目标页 `scrollY=0`、`document.activeElement=BODY`、focus 环 0 个、高亮元素 0 个、红边框输入框 0 个。全仓 grep:`useSearchParams` 只出现在 `login.tsx`,没有一个内容页读 `location.search`。
- **复现**:造 E1 红项(`downloads.ios.enabled=true` 且 URL 空)→ `/admin/publish` → 点【去修复】。
- **影响**:PRD CON13-⑥「红项『去修复』→ 对应内容页 → **定位到红字段**」仍未实现;相比上一轮唯一的变化是 URL 上多了一串没人读的参数。两条红项(ios / android)指向同一页、只有 `focus` 值不同,落地后**分不出是为哪一条来的**——而这正是 focus 本该解决的事。缓解:目标页自带页内红字提示两条都列出来了,运营不至于迷路。
- **根因**:产出端 `admin/src/pages/publish.tsx:308`;消费端不存在。

### P2-2 `humanPath()` 的尾巴仍是字段名;数组路径下把**整条原始路径**原样保留

- **页面**:`/publish`。**现象**:实测输出——`下载入口 · ios`(可读)、`Legal 文本 · 中文 · terms`(可读)、`公告条 · 英文 · text`(尾巴是字段名 `text`,但该页每语言只有一个正文框,勉强不歧义)、`平台数字 · activeDevices`(**该页把这个字段标为「Active devices(活跃设备)」,两处对不上**)、`文案 · 中文 · hero.note`、`页脚 · legalLine`、**`产品卡 · skus[0].priceUSD`**(人话行与下方小字行**是同一串**,前缀之外零信息增量,而且没说是哪一张产品卡)。
- **复现**:改一个 SKU 价格 → `/admin/publish` 看 diff 摘要表(截图 `sku-confirm.png`)。
- **影响**:主人点名要我判的第二件事。我的判断是**不够,而且不均匀**:对 `downloads` / `legal` 这类对象路径够用;对 `stats.*` 因为和页面标签对不上会让运营找不到要改哪一格;对 `skus[…]` / `faq[…]` 这类数组路径**等于没翻译**。缓解:文案树页面把原始 key 当卡片标题印(`content.tsx:100`),所以 `hero.note` 至少可以直接搜到。
- **根因**:`admin/src/pages/publish.tsx:60` `const rest = path.replace(/^[a-z]+\./i, '');` —— 这个剥前缀的正则要求首词后紧跟一个点,`skus[0]…` 永远不匹配,于是整条路径原样落进「人话」里;另外表里也没有把 `[n]` 解成产品名/问题标题的机制。

### P2-3 `coming-soon` 在三处仍然直出(徽标改了,同页另外两处和服务端消息没改)

- **页面**:`/content/downloads` ×2、`/publish` ×1。**现象**:
  1. 页内说明条:「未配置/关闭的入口,站上自动降级为 **coming-soon** 禁用态,零死链(WEB02)」——就在刚改好的徽标下面一行,同屏出现「即将推出」和 `coming-soon` 两种说法;
  2. 页内红字:「ios:开启的入口必须有 URL(或关闭改 **coming-soon**)」;
  3. `/publish` E1 红项的说明列:「开启的入口必须有 URL,或关闭改 **coming-soon**」。
- **复现**:打开 `/admin/content/downloads` 见 1;把 `downloads.ios.enabled` 置 true 且 URL 留空,见 2 和 3。
- **影响**:典型的「修一处 ≠ 修全部」。同一页上半句中文下半句机器词,反而比全用机器词更像没做完。另外 `WEB02` 是内部规格编号,也属于页面上不该出现的工程名词。
- **根因**:`admin/src/pages/downloads.tsx:46`(说明条)、`:26`(页内红字)、`schema/src/validators.ts:109`(服务端校验消息,`/publish` 与内容页同源)。

### P2-4 「改动数」两处算不准:线上那一行恒为空;窗口最旧那一行拿线上版当基线

- **页面**:`/publish` 版本表。**现象**:47 个版本时,①**线上 v2 那一行的改动数永远是 `—`**;②我把 v17 改成与 v18 差 5 个字段后,界面仍给 v18 印「**1 处**」——那是 v18 与 **v2** 的差,不是与它真前驱 v17 的差。
- **复现**:灌到 47 个版本触发截断 → 改 v17 的 payload → 打开 `/admin/publish` 或读 `GET /api/publish/status`。
- **影响**:这条修补自己写的口径是「算不出来就留空,**不编一个数**」。①是留空(符合口径),但恰好留空的是运营最想看数的那一行;②则是**编了一个数**——它算得出来、也印出来了,只是基线不对,而且从界面上看不出来。
- **根因**:`worker/src/publish.ts:525-533` —— 把 live 行 `push` 进 `versions` **之后**才 `sort((a,b)=>a.id-b.id)`,于是 `ordered[0]` 是被钉进来的线上版(id 最小),循环从 `i=1` 起:`ordered[0]` 永远拿不到值(→ 线上行空),`ordered[1]`(窗口最旧那行)的前驱被算成了线上版。

### P2-5 版本行「查看」→ 展开该版 diff 仍然缺席

- **页面**:`/publish`。**现象**:表头是「版本 / 时间 / 状态 / **改动数** / 理由 · 失败原因 / 操作」——改动数补上了;操作列只有【回滚到此版】,全表 `查看` 字样 0 处。
- **复现**:打开 `/admin/publish` 看版本表。
- **影响**:PRD CON13-⑥ 点击流矩阵有「版本行『查看』→ 当前页 → 展开该版 diff 摘要」。运营现在知道某版改了几处,但看不到改了**哪几处**——只能靠理由栏那一句话。上一轮 P2-1 的两半修好了一半。
- **根因**:`admin/src/pages/publish.tsx:361`(表头)、`:371-377`(操作列只渲染回滚按钮)。
- **附**:操作列的空/非空是对的——v48(已取消)与 v47(失败)没有回滚按钮,只有 `archived` 版本才有,和服务端同判据。

### P2-6 窄窗折叠成的是图标轨,不是 PRD 说的「抽屉」(没有展开开关)

- **页面**:全部页面(壳层),≤1023px。**现象**:侧栏收成 64px 固定图标轨,`aria-label`/`title` 都有(P1 已修),但**没有任何展开/收起控件**(实测匹配「菜单 / 导航 / ☰ / 展开 / drawer」的按钮 0 个),运营在窄屏下无法看到文字标签。
- **复现**:任意页面把窗口调到 900px 宽。
- **影响**:PRD CON02-⑤ 写「窄窗(<1024px)导航折叠为**抽屉**」。图标轨是个合理的替代形态、也不破版(四档实测均无横向溢出),但与规格不同,且鼠标必须悬停才能读到 `title`。记录为规格差。
- **根因**:`admin/src/styles.css:96`(只做隐藏标签)+ `admin/src/shell.tsx` 无抽屉状态。

### P2-7 驾驶舱骨架屏只覆盖真实五行里的前三行

- **页面**:`/`。**现象**:骨架 = 4/3/3 三行、10 张卡、20 条骨架、0 个转圈、`main` 高 **900**;真实 = 4/3/3/**4/2** 五行、**16** 张卡、高 **1129**。
- **复现**:延迟 `/api/dash**` 8 秒后打开 `/admin/`。
- **影响**:比上一轮(4 条挤在同一行 / 高 900 vs 1079)明显好转,但数据落地时仍跳 229px、下方 6 张卡凭空冒出来。骨架的作用是把版面占住,现在占住了六成。
- **根因**:`admin/src/pages/dashboard.tsx:84` 的 `([4, 3, 3] as const)` 与下方真实结构不同步(该处注释已提醒「改真实布局时这里要一起改」,但当前就已经对不上)。

### P2-8 审计页完全没用上那张共享映射表:变更列直印字段路径与 `key=value`

- **页面**:`/audit`。**现象**:变更列实景印出 `1 处改动:stats.nodes`、`enabled=false [CN] page=206B → enabled=true [CN] pag…`;对象列印 `legal.terms.zh`、`geo:rules`、`draft`。
- **复现**:登录 → 改一次区域屏蔽规则、存一次草稿 → `/admin/audit`(截图 `audit-0-default.png`)。
- **影响**:第 11 条修补抽的 `humanPath()` 是**导出**的共享映射表,但全仓只有 `publish.tsx` 自己的三个调用点在用它。审计页是给非工程角色看的追责面,却是全站原始路径最密集的一页。`enabled=…[CN] page=206B` 这种写法尤其像日志片段而不是给人读的记录。
- **根因**:消费端 `admin/src/pages/audit.tsx:91`(对象)与 `:100`(变更)不 import `humanPath`;生产端 `worker/src/config.ts:143`(`N 处改动:<原始路径>`)与 `worker/src/geo.ts:270`(`enabled=… [..] page=…B`)在服务端就把摘要写成了机器串。

### P2-9 区域屏蔽的 E2 误伤护栏仍靠「发一个注定失败的请求」试探,正常操作路径在 console 留一条红

- **页面**:`/geo`。**现象**:把高流量国家加进名单并点确认 → `PUT /api/geo` 返回 **409**(`need-confirm-high-traffic`),界面据此弹护栏面。功能完全正确,但浏览器 console 因此留下一条 error,网络面留下一条 409。
- **复现**:给某国造 ≥5% 的近 7 天流量 → 在 `/admin/geo` 把它加进名单 → 确认。
- **影响**:这正是第 10 条修补在发布/取消那边刚刚消灭的模式(改成由服务端在 `/status` 里直说 `cancelable`)。同一个反模式在 geo 这边原样保留,两处做法不一致。`GET /api/geo` 已经在回统计数据,把「哪些国家超阈值」一并回来即可用同一种修法。
- **根因**:`admin/src/pages/geo.tsx:54-56`(靠 catch 409 拿 `hot`)+ `worker/src/geo.ts:259-261`(只在 PUT 时算 `shares`,GET 不回)。

### P2-10 导出的 CSV 与屏幕上的表不是一份东西

- **页面**:`/audit`。**现象**:屏幕表头是「时间 / 动作 / 对象 / 变更 / 理由」,动作是中文人话、时间是本地化格式;CSV 表头是 `id,time,actor,action,target,before,after,reason`(英文),动作是机器码 `login.success`,时间是 ISO `2026-09-01T02:01:11.931Z`,还多一列 `actor`、变更拆成 `before`/`after` 两列。
- **复现**:`/admin/audit` → 点【导出 CSV】→ 打开文件比对。
- **影响**:BOM、转义、中文完整性这些真正容易出错的地方都做对了(见第一部分)。问题只在口径:运营把 CSV 发给法务/合规时,拿到的是一份需要工程翻译的表。PRD CON14-A1 说的是「列表显示 时间/动作/对象/before→after 摘要/理由;**可导出 CSV**」,读起来导出的应当是同一份。
- **根因**:`admin/src/pages/audit.tsx:55-56`(直接拼原始行,不过 `ACTION_LABEL`)。

### P2-11 未初始化时 `/login` 仍渲染完整口令表单,且试错会烧掉登录限速额度

- **页面**:`/login`(全新部署,尚未 setup)。**现象**:页面与已初始化时**完全一样**:口令框 1 个、【登录】按钮 1 个,只有页脚一行小字「首次使用?先完成初始化(部署时的 SETUP_TOKEN):去初始化 →」。
- **复现**:全新 D1(不跑 setup)→ 打开 `/admin/login`。
- **影响**:PRD CON01-⑤ 空状态写「未初始化访问 /login → 引导文案『先完成初始化』(**不渲染表单**)」。现状下,刚部署完的管理员会很自然地去猜口令,拿到的是「用户名或口令不正确」——而其实**根本没有账号**。更麻烦的是每次失败都记进限速窗口(`worker/src/auth.ts` 按 IP 计,5 次锁 15 分钟),而成功初始化只清 `setup:<ip>` 那把键、**不清登录键**,于是可能出现「刚建好账号却被锁在门外 15 分钟」。一次性、可自愈,但发生在第一印象上。
- **根因**:`admin/src/pages/login.tsx` 无 `/api/auth/state` 探针(隔壁 `setup.tsx:16-20` 就有一个现成的)。

### P2-12 改完区域屏蔽规则后,壳顶「屏蔽」chip 不刷新,要换页才更新

- **页面**:`/geo` → 壳层状态条。**现象**:在 geo 页把屏蔽从关改成开、toast 已提示「已写入并回读确认」、页内徽标已变「已生效 · 回读确认」,而**壳顶 chip 仍写「屏蔽 未启用」**;跳到任意别的页面后才变成「屏蔽 开启 · 1 个地区」。
- **复现**:`/admin/geo` → 开总开关 → 应用变更 → 不离开本页看状态条。
- **影响**:CON02 说状态条是「在任何页面都看到……屏蔽开关状态」的全局真相面,而刚改完它的那一刻它是错的。运营在这一屏上同时看到「已生效」和「未启用」。
- **根因**:`admin/src/pages/geo.tsx:52` 成功后只 `load()` 本页,没有调壳层暴露的 `reload()`(`useShell()` 里就有,`publish.tsx:105` 是这么用的)。

### P2-13 `gate-console-copy` 判据②只认 `{x.status}` 这一种形状,本轮抓到的枚举泄漏全在它盲区

- **面**:门本身。**现象**:判据②的匹配式是 `\{\s*([A-Za-z_$][\w.$]*)\.(status|action|state|kind|type)\s*\}`——只抓「把某个对象的这几个字段名直接渲染」。而本轮实景抓到的四处泄漏,它一处都看不见:
  - `coming-soon` ×3 —— 写在**字符串字面量**里(`downloads.tsx:46/26`、`validators.ts:109`);
  - `unknown` —— 渲染的是 `{r.target}`,字段名不在白名单里;
  - 顺带一提,上一轮修好的公告条那处本来也在盲区(`{winState}` 是个局部变量)。
- **影响**:门是真的、跑得动、接进链了(1b 已证实),但它守住的是**一种写法**而不是**一件事**。下一轮如果据此认为「枚举直出这一族已封住」,会再放过同类。
- **根因**:`worker/gate-console-copy.mjs:53-60`。可行的加强方向:对 JSX 文本节点里出现的已知机器词表(`coming-soon`/`disabled`/`active`/`unknown`/`cancelled`…)做词表扫描,而不是只看表达式形状——那样上面四处会全部命中。

### P2-14 未登录首屏仍有 1 条 console error(如实记录,较上轮 8 条已大幅下降)

- **页面**:`/admin/`(未登录)。**现象**:`error: Failed to load resource: the server responded with a status of 401 (Unauthorized)`,来自 `/api/me` 会话探针;失败请求 0 条、`ERR_ABORTED` 0 条、整页重载 0 次。
- **影响**:这条是浏览器对 401 响应自己打的,应用侧无法抑制(除非把探针改成不返 401 的形式)。**记录备查,我不认为需要改**;放在这里只为让「console 条数」这个数字在下一轮有可比的基线:登录后全程走查 console 恒 **0**。

---

## 六、没能走到的面(不静默略过)

| 项 | 为什么 |
|---|---|
| **审计页真·空结果态 + 空表导出** | 本轮六个过滤器每一个都至少命中 1 行(全部 16 / 内容 4 / 规则 1 / 登录 9 / 账号 1 / 运维 1),造不出零命中;「没有匹配记录」与「当前无可导出记录」两条走的是上一轮已验过的分支,本轮**只从代码上确认分支还在**(`audit.tsx:82` / `:53`),没有实景。 |
| **区域屏蔽 E1 自锁保护 / 直通 cookie 全链路** | 【获取直通】按钮实测可点(`bypassAvailable=true`),但它会新开标签跳站域兑换 30 天 cookie;本轮没有走完兑换后「从被屏蔽地区仍放行且不计统计」的验证。 |
| **区域屏蔽 E3 兜底名单告警条** | 需要让 KV 读取真失败;本轮没有造这个故障,`degraded` 一直是 false,「边缘正在使用内置兜底名单」那条告警未在界面上见过。 |
| **门真红时的失败面(真跑一次红门)** | 本轮真发布是**绿的**(13 门全过)。失败面是用种子数据摆出来的(v47 带 `render-fit` 大白话原因),**「门在真实时序下判红 → 界面怎么变」没有实景**。要补需故意写一段撑破版面的文案再跑一次 5 分半的发布。 |
| **`/setup` 的 429 限速态** | 只用了 1 次错令牌(限速是 5 次/15 分钟),没有把 `setup:<ip>` 窗口打满;那一支(「尝试过多,稍后再试」)未在界面上见过。 |
| **多语言 / 多浏览器 / 移动端真机** | 控制台是单语中文,仅测 Chromium 桌面视口(含 768–1440 五档模拟宽度)。 |

---

## 七、收尾状态(我把环境留成了什么样)

- **服务**:已停。杀 wrangler 监督进程树(PowerShell `Stop-Process`,不用 `taskkill /PID`),回读 **8823 监听数 = 0**,`workerd` 无残留。**4399(别的会话的)全程未动**。
- **仓库**:`git status` **完全干净**,HEAD 仍是 `f21bc6d`。执行器物化步写过的 `src/i18n/{en,vi,zh}.json` 与 `src/config/site.json` 已 `git checkout` 还原;冻结面(`worker/src` / `admin/src` / `worker/*.mjs` / `src/components/legal`)`git diff --stat` 为空。
- **本地 D1 / KV**:留在 `…/scratchpad/wstate8`(独立 `--persist-to`),**未触碰** `worker/.wrangler/`。里面是走查造出来的局面:49 个版本(v2 是线上,v48/v49 已取消,若干 failed/cancelled/`{}` payload)、区域屏蔽**已开启且名单含 CN**、17 条真拦截事件 + 当日汇总、VN/US/SG 的 7 天流量行、一条超长审计行,草稿已复位为与线上一致(`dirty=0`、`drift=null`)。**删掉整个 `wstate8` 目录即完全重置**,对仓库无影响。
- **构建产物**:`dist/` 与 `dist-live/` 是本轮真发布(v2)留下的。`dist-live/.publish-stamp.json` 我为了造劈叉态改过两条 anchor,**验完已从备份原样还原**,当前 `drift=null`。
- **留下的东西**:走查脚本 `r8lib.mjs` + `s1`–`s14`、68 张截图、17 份 JSON、导出的 `audit-2026-09-01.csv`,全在 `C:\Users\jason\AppData\Local\Temp\claude\D--WORKS-PLAN\77632ed7-c975-44f2-9faa-7d4a48266c50\scratchpad\`。不在仓库内。

---

## 八、给主人的一句话

| 做了啥 | 结果咋样 | 要主人拍板啥 |
|---|---|---|
| 补走了上一轮明说没走到的 5 个面(初始化页四态 / 真跑一次完整发布 / 屏蔽卡有真数据 / CSV 真下载 / 文案树 18 组逐个展开),再把这一轮 12 条修补拆成 22 个子断言逐条回核 | **不可签字**:P0 零条、**P1 三条**、P2 十四条。补走的 5 项**全部通过**——真发布 5 分 30 秒四步推进、耗时用人话、完成后状态条与红条同步翻篇,是本轮最扎实的一段;22 个子断言里 17 条实景证实。挡住签字的是三条 P1,其中**一条是这轮修补自己引入的回归** | 见下 |

**主人点名要我判的两件事,先答**:

1. **「去修复」的 `?focus=` 有没有用?** —— **没有用,是个摆设**。链接确实带上了参数,但内容页里没有一行代码读它(点击后不滚动、不聚焦、不高亮,全仓 grep 零消费者)。好消息是目标页自带页内红字,运营不至于迷路;坏消息是有两条红项时,落地后分不清是为哪一条来的。
2. **`humanPath()` 的尾巴仍是英文字段名,够不够运营用?** —— **不够,而且不均匀**。`下载入口 · ios` 够用;`公告条 · 英文 · text` 勉强(该页每语言只有一个正文框);`平台数字 · activeDevices` 不够(页面上那一格叫「Active devices(活跃设备)」,对不上);**`产品卡 · skus[0].priceUSD` 等于没翻译**——人话行和下面的小字行是同一串,还不告诉你是哪一张产品卡。根因很具体:剥前缀的正则要求首词后紧跟一个点,数组路径 `skus[0]…` 永远不匹配。

**要拍板的一件事:这三条 P1 怎么排?**

- **选项 A(三条一起修,再压一轮)**:P1-1 是把 `shell.tsx:56-60` 的 `.catch(() => {})` 改成也置位 `failed`(一行);P1-2 是把 `validators.ts:164` 的正则从点号改成兼容方括号(一行,但**必须配一条红测**,否则下次换个路径形状又会静默失效);P1-3 要决定「对象」列对登录事件到底该写什么(见下)。代价:半天,加一轮验收。
- **选项 B(先合并主线,单开修复包)**:发布链本身是好的,主线早点拿到;代价是带着「后端一挂就永远转圈」和「改产品价格不用写理由」进主线。
- **我的推荐:A,但 P1-3 拆出去单独问。** 理由:P1-1 和 P1-2 都是一行改动 + 一条测试,风险低、当轮可验,而且各自都有明确的机器门可焊(壳层报错态可以写成组件测试;高敏路径可以焊一条「`sensitivePaths` 必须对 `flattenJson` 真实产出的路径样本有命中」的红测——**这条比修正则本身更值,因为它守的是「两边路径写法必须对得上」这件事,而不是某一条正则**)。P1-3 不是改代码的问题,是「登录事件的『对象』应该写账号还是写来源 IP」这个口径没定,写完还要考虑上线后 IP 入库的隐私口径,不该在修 bug 的同一轮里顺手决定。
- **不做会怎样**:P1-2 是唯一有长期后果的一条——产品价格、型号名、算力倍数改上线后**永远查不到是谁为什么改的**,而且这个洞是静默的,没有任何界面迹象。P1-1 会在真出故障那天让运营以为是自己网卡了(它恰好只在后端真挂的时候发作)。P1-3 是观感与追责可读性。

**主人可自验(3 步)**:

1. 仓根 `npm run build && npm run build:console`,`cd worker && node promote.mjs`,`npx wrangler dev --port 8823`,浏览器开 `http://127.0.0.1:8823/admin/`(首次用 `dev-setup-token` 初始化)。
2. 点左侧「产品卡」,把任一 SKU 的价格改一个数 → 保存草稿 → 点左侧「发布与版本」。**应该看到**:标题只写「本次发布 · 1 处改动」、没有「含高敏字段,须填理由」;点【发布】弹出的确认框里**连理由输入框都没有**——而产品卡页标题明写着「事实字段高敏」。
3. 把 worker 那个终端直接 Ctrl+C 关掉,然后刷新控制台任意页面。**应该看到**:页面永远停在几块灰色骨架上,没有「状态获取失败」,也没有任何重试按钮。
