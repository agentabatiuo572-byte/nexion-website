# R45 独立评审 · 可读性与可用性(真实访客视角)

评审对象:`http://localhost:4399/` 构建产物(2026-08-26,UTC 05:31–05:45 采样)。角度:看得清、点得到、动效不添堵。
结论先行:**7.9 / 10**。站点整体可读性扎实(纸面正文 5.3–10:1、导航磨砂底板静态 ≥16.9:1、CLS=0、四档视口零溢出、慢网零闪现、键盘全程有焦点环),扣分集中在四件真实访客会撞到的事:① 导航翻色瞬切在两种滚动情形下有 4 帧(≈66ms)文字对底板跌到 1.68:1;② 本轮新增的文章目录/相关文章小字直接压在粒子线上(局部 <4.5:1 面积 11–29%,站规「小字禁直压粒子线」);③ 手机端证书灯箱只放大 10%(312→342px),证书正文仍不可读,且触屏没有任何「可放大」提示;④ How 第 3 步行内链 44px 命中区盖住上一行正文。

## 方法

- Playwright(借 `Nexion-uniapp` 依赖)真浏览器,脚本 `scratchpad/rev3-*.mjs`,原始数据 `rev3-*.json`,截图 `rev3-*.png`(均在会话 scratchpad,路径见文末)。
- 视口:1440×900 · 1366×768 / 1366×673 · 1440×700 · 1536×774 · 1920×1080 · 2560×1440 · 390×844(hasTouch+isMobile)· 844×390 横屏;平板过渡 1100 / 1024 / 900 / 861 / 768;经典滚动条模式 `ignoreDefaultArgs:['--hide-scrollbars']` 1440 / 1600 / 1920。三语首页 + `/learn/` `/learn/getting-started/` `/nex/` `/legal/privacy/` + 404 + 无 JS(390)+ reduced-motion + print。
- 慢网:CDP `emulateNetworkConditions` 1.5 Mbps / 300 ms(另加 400 kbps / 800 ms 一组),`addInitScript` 逐帧记录导航 / 标题 / 副题 / 下载键 / 画布的计算样式(不靠截图)。
- 对比度:按 WCAG 相对亮度自算,alpha 合成到实际底色。**粒子上的文字**用像素法:隐藏文字元素→截暗区(可信)→逐像素把文字色合成到该像素→算对比→统计「<4.5:1 面积占比 / 最差值 / 均值」,每处取 3 帧(流光在动)。**磨砂底板**同法再加 σ=14 的高斯近似模糊后合成 tint。**翻色过程**用 rAF 逐帧读 `color` 与 `::before` 的 `background-color`,按白带边缘相对文字行的位置判底(白 / 黑)合成。亮区一律用计算样式,不用截图判像素。
- 滚动一律 `mouse.wheel`(Lenis);hover 探针在捕获阶段拦截 click 防导航。

## 访客路径观察

| 步骤 | 实测 | 判定 |
|---|---|---|
| 首屏(1440×900,快网) | 画布 94→739ms 淡入;导航 506→1006ms;标题两行 605ms 滑入(遮罩建好前不可见,`h1AnyVisibleBeforeReady=false`);副题+下载键 1507→1922ms。下载键整行在首屏内(底 673 ≤ 900)。刷新到 y=5200 / 后退:导航第 0 帧即 opacity 1,不重放 | 符合拍板的四拍;CTA 要等 1.9s 才出现属既定编排(已排除) |
| 首屏(慢网 1.5Mbps/300ms) | CSS 到达前空白 0.79s(en)/0.88s(vi);无一帧无样式(`unstyledFrames=0`,`foucNav=0`);导航 1.24→1.73s,标题 1.42s(字体已就绪,Funnel/Be Vietnam 正体),副题+CTA 2.23→2.65s;fx 0.88s 到达(2G 档 1.77s),35 个打字机 / 首屏标题 0 帧闪现 | 通过 |
| 滚读 | 整页滚轮读完 CLS **0.000**;3 处黑区导航磨砂底板最差 16.9:1(hero/mission/deck/final/footer 六位抽样,`pctBelow45=0`);白带上黑字对底板理论最差 9.9:1 | 通过 |
| 导航过白带(翻色) | 7 种进出情形逐帧:wheel 进 4.77 / wheel 出下缘 4.13 / 从下缘进 4.13 均 ≥3:1;**慢速进(白带顶缘刚过导航底缘,导航整条压在黑区)最低 1.68:1,4 帧 <3:1**;**滚轮向上退出白带 最低 1.68:1,4 帧 <3:1**。机制:底板半透明,翻色时的实际颜色取决于压着的是黑区还是白带,而文字瞬切点按「底板压白带 / 压黑区」各只算了一种 | 扣分 #1 |
| 手机菜单(390×844) | 汉堡 44×44,开态两杠成 ×;菜单全屏实底 #0c0c0d,五链 44px display 单行(vi「Cách hoạt động」宽 339 ≤ 390),语言链 35×44,下载键 120×48;空白四点 `elementFromPoint` 全在菜单内,LOGO `visibility:hidden`;开着时滚轮页面位移 0;Tab 9 项后回汉堡循环;Esc 关闭且焦点回汉堡;点链接关闭并落到 `#how`(标题距导航底 10px);白带上汉堡为墨色 `rgb(12,12,13)`。横屏 844×390:菜单 `overflow-y:auto`,滚轮 / 触摸拖动都能把下载键拉进视口,页面不动 | 通过 |
| 证书灯箱 | 桌面:点击开,焦点落关闭键(74×44);滚轮 → `dialog.scrollTop` 204(到底),`scrollY` 不变;关闭键常驻可见;Esc / 背景点击 / 键盘 Enter 开启均可,关闭后焦点回缩略图;关闭后滚轮页面恢复滚动;遮罩实测 242→54(0.82 黑)。图 1160 原件,桌面显示 720px(2560 下 971px),证书正文可读。**手机**:对话框 374×545,图 **342px**(缩略图 312px,放大 1.10×),证书正文字高 ≈3px;`cert-hint` 仅 hover 出现,触屏无提示 | 扣分 #3 |
| 学习中心 / 文章 | `/learn/` 卡纸面 5.33:1,hover 0.92 → 卡亮度 0.83→0.69 可辨;文章 TOC 点击 → 标题距导航 10px,焦点落 H2,再 Tab 落相关文章;返回链 hover 变全白;正文 12px/1.85 纸面 5.33:1,86 字/行(行长已排除);相关文章 2 条 60px 高。**但**文章页头部 meta 行、TOC 标签、相关文章标签 / 箭头都直接压在粒子线上(像素法 <4.5:1 面积 11–29%,最差 1.23:1) | 扣分 #2 |
| 页脚(透底) | 免责声明 12px/0.62 白:均值 7.35:1,流光穿过时 1.17% 面积 <4.5:1(最差 1.46:1);法务链最差 4.49:1(0.02%);版权行 OK;hover 0.62→1 可辨 | 已拍板透底,不扣分(见已排除附注) |
| 键盘 | Tab 序:LOGO→5 链→3 语言→下载→滚动提示→内容;焦点环 2px 柠檬(白带上自动深柠檬 #486405);Tab 到屏外元素浏览器平滑滚动跟随(1.5s 内进视口);锚点点击后目标获焦、下一 Tab 落区内 | 通过;无「跳到内容」链(既有,见 #7) |
| 视口矩阵 | 1366×673 / 1366×673-vi / 1440×700-vi / 1536×774 下载键底 645 / 645 / 673 / 718 均在首屏;vi 三行标题顶距导航 171–186px;2560 画布 1920 居中、白带全出血 0→2545、数字条随副题拍在首屏;1920 同;平板 861–1100 导航单行不溢出,768 起汉堡;经典滚动条 1440 / 1600 / 1920 画布左右空档 0 / 0、白带右缘 = clientWidth、无横向滚动 | 通过 |
| 三语 | zh mono 12.5/1.6、副题 16.56/1.5;vi 首页标题 Be Vietnam Pro 三行不压导航;三语手机首屏 / 菜单 / 溢出扫描全 0 | 通过 |
| 无 JS / reduced / print | 无 JS 390:五链随流可见(导航 186px 高)、内容全显、证书 `href=/cert-msb@2x.png`;reduced:全部直显、叠卡竖排;print 法务页白底黑字 8.5:1、导航 / 粒子隐藏 | 通过 |

## 扣分清单

| # | 严重度 | 类型 | 问题 | 证据 | 参数级修法 | 扣分 |
|---|---|---|---|---|---|---|
| 1 | P2 | 新增缺陷(本轮 B5 瞬切机制) | 导航翻色两种情形文字短暂不可读:(a) 慢速滚进白带——触发时导航整条仍压黑区,底板 54ms 后文字切黑,底板此时合成色 rgb(56) → **1.68:1**,随后 1.99 / 2.36 / 2.82,共 4 帧 <3:1(≈66ms);(b) 滚轮向上退出白带——文字要等 196ms 才切白,底板在黑区上变暗,同样 4 帧 1.68–2.82:1。T3 AC2「任一帧 ≥3:1」未达 | `rev3-desk1.json › navTransitions[0]`(slow entry)与 `[3]`(wheel exit upward),逐帧 `plate/c` 列;其余 5 种情形最低 4.13–4.77 | 翻色窗内让底板不透明:JS 已在 `recolor()` 切类,同一处加 `flipping` 类保持 300ms,`.site-nav.flipping::before{ background-color: rgba(<当前 tint>, .95) }`,文字瞬切点按不透明底板的亮度交叉点(≈0.19)算,两向都 ≥4.4:1、与压黑压白无关;不动静态 0.64 / 0.72(磨砂观感不变)。次选:把阈值从「导航底+10」挪到文字行中线并按方向分设延迟(只治快滚,慢滚仍会露) | −0.4 |
| 2 | P1 | 新增缺陷(本轮 A11/B7 内页归一新增块)+ 既有同族 | 内页头部与相关文章的小字直接压粒子线,违站规「小字密集板块必须不透明底」。**新增**:`.toc .x-label`「IN THIS GUIDE」<4.5:1 面积 12.8%(最差 1.25);`.related .x-label`「RELATED GUIDES」11.4%(1.25);`.related .arr` 28.6% / 0%(3.06);相关文章标题 24px 0.8%。**既有同族**:文章 `.meta` 13.6%(1.23);`/learn/` 副题 11.4%(1.21,<3:1 2.8%);`/nex/` `.lead` 10.0%;`/legal/` `.updated` 11.1%(<3:1 3.6%)。均值 6.9–7.1 仍可读,但 1440×900 下这些 12px 行恰在吸引子左叶密线带里,不是只有流光经过才坏 | `rev3-particles.json › article.top / article.related / learn / nex / legal`;截图 `rev3-article-related-1440.png`(流光穿过「Turning on phone compute」与箭头)、`rev3-article-top-1440.png` | 内页头部块(back/meta/TOC)与相关文章块给局部实底:`background: var(--x-bg)` + 内衬 16–24px(或并入正文 `.x-paper` 纸面,与目录同纸);`/learn/` `/nex/` `/legal/` 头部副题 / 更新行同法。不必动粒子姿态 | −0.5(新增)−0.2(既有) |
| 3 | P2 | 新增缺陷(本轮 A4 灯箱) | 手机端灯箱放大量只有 +10%(缩略 312 → 灯箱 342px),对话框 374×545 无内滚,证书正文字高≈3px 仍不可读;触屏无 hover,`cert-hint`「View full size」opacity 0,访客不知道能点 | `rev3-mobile.json › en.cert`(thumb 312×416,img 342×457,scrollH=clientH=545);截图 `rev3-m-en-lightbox.png` | ≤860 / `(hover:none)`:对话框全出血 `max-width:100vw; max-height:100vh; padding:0`,图 `width:max(100%, 1160px)` + `overflow:auto` 可平移(关闭行已 sticky),或至少 `img{width:200%}`;`@media (hover:none){ .cert-hint{opacity:1} }` 常显提示 | −0.4 |
| 4 | P2 | 新增缺陷(本轮 A12 行内链命中 44) | How 第 3 步行内链上下各垫 13.5px 负边距抵消,行高只有 17.4px → 命中盒盖住上一行「in-app balance.」:该行文字上 hover 出现指针 + 链接变淡,点击即跳 `#trust` | `rev3-desk3.json › vlink`:`hitAt4pxFromTop=vlink`,`hitOneLineAbove=vlink`,rect 196×44 vs 行高 17.4 | 垫高收到 6px(命中 ≈30px,不侵入邻行),或链接独立成行 `display:block; min-height:44px`(版面多一行,零侵入) | −0.2 |
| 5 | P2 | 既有值(StatsBar 未在本轮改样式) | 数字条第 3 格标签「NETWORK NODES」12px/0.62 白压在吸引子中心密线上:<4.5:1 面积 **56.5%**,<3:1 5.9%,最差 1.43,均值 4.67;同区 `asof` 行 2.1% | `rev3-particles.json › home.stats` | 数字条整体加实底 `background: var(--x-bg)`(它本就是全出血暗带),或标签升 `--x-ink-cap` 0.8 + 文字阴影 `0 0 6px var(--x-bg)` | −0.2 |
| 6 | P3 | 既有值 | 桌面语言链 EN / VI 命中区只有 15×44(中文 24×44),无 padding,间距 10px;手机端已有 ±10 内衬。WCAG 2.5.8 靠间距例外勉强过,鼠标要瞄 | `rev3-desk1.json › tabs[6..8]`、`rev3-desk3.json › hover[1].rect` | `.lang a{ padding-inline: calc(6*var(--uc)); margin-inline: calc(-6*var(--uc)); }`(与 `.links a` 同法,版面零变化) | −0.1 |
| 7 | P3 | 既有值 | 键盘用户要过 10 个 Tab(LOGO+5 链+3 语言+下载)才到内容,无「跳到内容」链;FAQ / PoC `summary` 与滚动提示 hover 零反馈(只有指针),+/− 记号 7×14px | `rev3-desk1.json › tabs`,`rev3-desk3.json › hover[4,7,10]` | `<a class="x-sr" href="#main">`(focus 时显)一行;`summary:hover .q{opacity:.6}` 复用 dimlink 手势 | −0.1 |

合计 −2.1 → **7.9**。

## 已排除(主人已拍板 / 既定,不扣分)

- 首屏标题字号与文案;窄屏不做第二套画布;信任五卡 4+1;FAQ 右缘;正文行长(文章 86 字/行)与全大写;页脚空档;自定义光标;手机汉堡;reduced-motion 保留;排印档位数;导航小字 12px 下限与条高;画布 zoom;编号强调色;卡片淡入;桌面灯箱原尺寸;粒子性能。
- **导航底板半透明磨砂(本轮拍板)**:静态可读性实测无问题(黑区最差 16.9:1、白带 ≥9.9:1);扣分 #1 只针对翻色窗内的实现。
- **页脚透底(本轮拍板)**:免责声明流光穿过时 1.17% 面积 <4.5:1(最差 1.46),法务链最差 4.49——不扣分;若想在不改透底的前提下兜底,`.info` 加 `background: color-mix(in srgb, var(--x-bg) 70%, transparent)` 局部薄纱即可。
- **首屏底空 220(本轮拍板)**:1366×673 / 1440×700 / 1536×774 下载键均在首屏内,vi 标题顶距导航 ≥171px,实测达标。
- **首载四拍时序**:快网 CTA 1.9s、慢网 2.65s 才出现,是既定编排。
- **下载键禁用占位(既定)**:文字 7.72:1 可读,但描边 `--x-line-soft` 对底只有 1.19:1,三键几乎无边界(2560 与手机页脚截图可见)——既定不扣,建议描边升 `--x-line`。
- **手机端 12px 等宽正文**(首页 390 下 12px 文字 4295 字,占比最高):属排印档位 / 12px 下限拍板。

## 复核要点(供回源)

- #1 用 `rev3-desk1.mjs` 段 F 可复现:`jump(bandTop − navH − 14)` 后 `scrollTo(+8)`,逐帧读 `getComputedStyle(nav,'::before').backgroundColor` 与 `.links a` 的 `color`,按白带 `top` 相对文字行中线(y≈52)判底合成。
- #2 用 `rev3-particles.mjs`:隐藏元素→截 1440×900→逐像素合成;数值随流光位置每帧略变,3 帧取最差 / 最大占比。
- 证据文件:`C:/Users/jason/AppData/Local/Temp/claude/D--WORKS-PLAN/42e4397e-0bc4-4987-8cc3-a0bc8464e0bd/scratchpad/rev3-{desk1,desk2,desk3,mobile,sizes,slow,pages,follow,particles}.json` 与同名 `.mjs`;截图 `rev3-nav-*.png` `rev3-footer-1440.png` `rev3-lightbox-1440.png` `rev3-m-en-{menu,lightbox,how,footer}.png` `rev3-2560-hero.png` `rev3-article-{1440,top-1440,related-1440}.png` `rev3-learn-1440.png` `rev3-nex-{1440,top-1440}.png`。

## 总分

**7.9 / 10** — 静态可读性、点击命中、菜单 / 灯箱 / 键盘 / 慢网 / 多视口都过关;失分在翻色窗 4 帧不可读、本轮新增小字压粒子线、手机灯箱形同虚设、行内链命中越界这四处真实访客会碰到的地方,全部可参数级修。
